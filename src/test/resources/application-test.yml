spring:
  data:
    mongodb:
      uri: mongodb://localhost:27017/test
  
  redis:
    host: localhost
    port: 6379
  
  kafka:
    bootstrap-servers: localhost:9092

logging:
  level:
    root: WARN
    org.wespeak: DEBUG
