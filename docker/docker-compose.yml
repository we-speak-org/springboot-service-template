version: '3.8'

services:
  # MongoDB
  mongodb:
    image: mongo:7.0
    container_name: template-mongodb
    ports:
      - "27017:27017"
    environment:
      MONGO_INITDB_ROOT_USERNAME: admin
      MONGO_INITDB_ROOT_PASSWORD: admin
      MONGO_INITDB_DATABASE: template
    volumes:
      - mongodb_data:/data/db
    networks:
      - wespeak-network
    healthcheck:
      test: echo 'db.runCommand("ping").ok' | mongosh localhost:27017/test --quiet
      interval: 10s
      timeout: 5s
      retries: 5

  # Redis
  redis:
    image: redis:7.2-alpine
    container_name: template-redis
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    networks:
      - wespeak-network
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 3s
      retries: 5

  # Kafka & Zookeeper
  zookeeper:
    image: confluentinc/cp-zookeeper:7.5.3
    container_name: template-zookeeper
    environment:
      ZOOKEEPER_CLIENT_PORT: 2181
      ZOOKEEPER_TICK_TIME: 2000
    networks:
      - wespeak-network
    healthcheck:
      test: nc -z localhost 2181 || exit -1
      interval: 10s
      timeout: 5s
      retries: 5

  kafka:
    image: confluentinc/cp-kafka:7.5.3
    container_name: template-kafka
    depends_on:
      zookeeper:
        condition: service_healthy
    ports:
      - "9092:9092"
    environment:
      KAFKA_BROKER_ID: 1
      KAFKA_ZOOKEEPER_CONNECT: zookeeper:2181
      KAFKA_ADVERTISED_LISTENERS: PLAINTEXT://localhost:9092
      KAFKA_OFFSETS_TOPIC_REPLICATION_FACTOR: 1
      KAFKA_AUTO_CREATE_TOPICS_ENABLE: 'true'
    networks:
      - wespeak-network
    healthcheck:
      test: kafka-broker-api-versions --bootstrap-server localhost:9092
      interval: 10s
      timeout: 5s
      retries: 5

  # Keycloak
  keycloak:
    image: quay.io/keycloak/keycloak:23.0
    container_name: template-keycloak
    command: start-dev
    ports:
      - "8080:8080"
    environment:
      KEYCLOAK_ADMIN: admin
      KEYCLOAK_ADMIN_PASSWORD: admin
      KC_DB: dev-file
    networks:
      - wespeak-network
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8080/health"]
      interval: 30s
      timeout: 10s
      retries: 5

  # Application (optionnel - décommenter pour tester l'image Docker)
  # template-service:
  #   build:
  #     context: ..
  #     dockerfile: docker/Dockerfile
  #   container_name: template-service
  #   depends_on:
  #     mongodb:
  #       condition: service_healthy
  #     redis:
  #       condition: service_healthy
  #     kafka:
  #       condition: service_healthy
  #     keycloak:
  #       condition: service_healthy
  #   ports:
  #     - "8081:8080"
  #   environment:
  #     SPRING_PROFILES_ACTIVE: dev
  #     MONGODB_URI: mongodb://admin:admin@mongodb:27017/template?authSource=admin
  #     REDIS_HOST: redis
  #     REDIS_PORT: 6379
  #     KAFKA_BOOTSTRAP_SERVERS: kafka:9092
  #     KEYCLOAK_ISSUER_URI: http://keycloak:8080/realms/wespeak
  #     KEYCLOAK_JWK_SET_URI: http://keycloak:8080/realms/wespeak/protocol/openid-connect/certs
  #   networks:
  #     - wespeak-network
  #   healthcheck:
  #     test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:8080/actuator/health"]
  #     interval: 30s
  #     timeout: 10s
  #     retries: 5

networks:
  wespeak-network:
    driver: bridge

volumes:
  mongodb_data:
  redis_data:
